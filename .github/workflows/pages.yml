name: Deploy Pages

on:
  push:
    branches: ["main"]

# Add this block right here
permissions:
  contents: read
  pages: write
  id-token: write

concurrently:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Your build and deploy jobs continue below...
